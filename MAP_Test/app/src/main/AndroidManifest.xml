<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.map_test">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/pencil"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pencil"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Test">

        </activity>
        <activity android:name=".Scratch">

        </activity>
        <activity android:name=".Puzzle" />
        <activity android:name=".MenuActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".CircleTest" />
        <activity android:name=".ChartTest" />
        <activity android:name=".RandomMap2" />
        <activity android:name=".RandomMap1" />
    </application>

</manifest>